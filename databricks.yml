bundle:
  name: lakehouse_federation_demo

sync:
  include:
    - "notebooks/federation_demo.sql"
  exclude:
    - "notebooks/federation_demo_template.sql"
    - "terraform/**"
    - "scripts/**"

targets:
  dev:
    default: true
    workspace:
      root_path: ~/.bundle/${bundle.name}
